PORT=3000
NODE_ENV=development
# Secret utilisé pour signer les sessions (JWT). Générez une valeur aléatoire
# forte pour la production, ex : node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
